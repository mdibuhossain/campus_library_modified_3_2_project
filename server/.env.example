NODE_EVN="development"
MONGO_URI=""
MONGO_URI_LOCAL="mongodb://127.0.0.1:27017"
DB_PASS=
DB_USER=
DB_NAME="campuslib_3_2"
FIREBASE_SERVICE_ACCOUNT=

# Shared secret for the deadline-reminder endpoint, called by an external free
# cron service (Vercel Cron on Hobby only allows one run per day).
# Generate any long random string, e.g. openssl rand -hex 24
CRON_SECRET=
